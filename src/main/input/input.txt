// Variables globales
int variableGlobal;
int variableGlobal; // ERROR: Variable duplicada en el mismo ámbito
double valorGlobal;
char caracterGlobal;

// Función con múltiples errores + IF / WHILE / lógicos
int miFuncion(int parametro1, double parametro2) {
    int variableLocal;
    int variableLocal; // ERROR: Variable duplicada en función
    
    // Variables nunca utilizadas (WARNING)
    int variableNoUsada1;
    string variableNoUsada2;
    double variableNoUsada3;
    
    parametro1 = 100; // OK: asignación a parámetro
    variableLocal = parametro1 + 5; // OK: uso de variable

    // IF con condición lógica
    if (parametro1 > 0 && parametro2 < 10.0) {
        variableLocal = variableLocal + 1;
    } else {
        variableLocal = variableLocal - 1;
    }

    // WHILE con condición relacional
    while (variableLocal < 120) {
        variableLocal = variableLocal + 2;
    }
    
    // ERROR: Asignación a variable no declarada
    variableFantasma = 42;
    
    // ERROR: Asignación a una función (no es variable)
    miFuncion = 10;
    
    // OK: usar variable global
    valorGlobal = 3.14;
    
    return variableLocal;
}

void funcionVoid() {
    int x;
    int y;
    int z; // WARNING: declarada pero no usada
    
    x = 10; // OK
    y = x + 5; // OK: uso de variables

    // IF simple con booleanos
    bool cond1;
    bool cond2;
    cond1 = true;
    cond2 = false;

    if (cond1 || cond2) {
        x = x + 1;
    }

    // WHILE con condición lógica
    while (x < 20 && !cond2) {
        x = x + 2;
    }
    
    // ERROR: variable no declarada
    w = x + y;
}

// Función main para completar el programa
int main() {
    int resultado;
    int valor;
    
    resultado = miFuncion(5, 3.14); // OK: uso de función
    valor = resultado + 10;         // OK: uso de variables
    
    // IF usando comparaciones y lógicos
    if (valor >= 10 && valor != 15) {
        variableGlobal = valor;
    } else {
        variableGlobal = 0;
    }

    // Bucle while simple
    while (variableGlobal < 100) {
        variableGlobal = variableGlobal + 5;
    }
    
    // ERROR: variable no declarada
    variableFinal = valor;
    
    // OK: usar variable global
    variableGlobal = valor;
    
    return resultado;
}
